// FILE: src/components/features/model-playground/Selector/SelectorDropdown.tsx
import { component$ } from '@builder.io/qwik';

export const SelectorDropdown = component$(() => {
  return (
    <div>
      {/* Minimal placeholder */}
      <p>SelectorDropdown placeholder</p>
    </div>
  );
});
